import {ShieldCheck,Lock,Database,EyeOff,KeyRound,CheckCircle2} from 'lucide-react'; export function SecurityPage(){const items=[['Private storage',Database,'Generated conversion artifacts use private storage policies.'],['Secure authentication',KeyRound,'Supabase Auth supports password and Google OAuth authentication.'],['Access control',Lock,'Row-level security restricts user data and conversion history to authorized users.'],['Minimal exposure',EyeOff,'The application avoids exposing private files through public buckets.'],['Security mindset',ShieldCheck,'Security is considered across authentication, storage and conversion workflows.']];return <div className="container-page py-16"><div className="max-w-3xl"><span className="section-eyebrow">SECURITY & DATA PROTECTION</span><h1 className="text-5xl font-extrabold mt-5">Your documents deserve a locked door.</h1><p className="text-lg text-ink-500 mt-5 leading-8">QuadraConverter is designed to reduce the privacy risks of sending sensitive documents through generic online workflows.</p></div><div className="grid md:grid-cols-2 gap-5 mt-12">{items.map(([t,I,d])=><div className="card p-7" key={String(t)}><I className="w-7 h-7 text-brand-700"/><h2 className="font-bold text-xl mt-4">{String(t)}</h2><p className="text-ink-500 mt-2 leading-6">{String(d)}</p></div>)}</div><div className="mt-10 p-6 rounded-2xl bg-amber-50 ring-1 ring-amber-200 text-amber-900 text-sm">Security controls depend on correct Supabase configuration, OAuth redirect settings, database RLS policies and production deployment settings. Never put Supabase service-role keys in the browser.</div></div>}
